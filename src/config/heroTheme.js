/*
  Experiment flag: renders the homepage hero on a dark forest ground instead
  of the light sand one, so the two can be compared on the real page. The
  Navbar reads it too, since it rides transparent over the hero on `/` and has
  to flip its text colour to stay legible.

  Flip to false to go back to the light hero. Once the call is made, delete
  this file and inline the winning branch.
*/
export const HERO_DARK = true;
